<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.joseph.sweepersd">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="com.google.android.gms.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".revision3.SweeperSDApplication"
        android:allowBackup="true"
        android:icon="@drawable/alert_sweeping_app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <service
            android:name=".revision3.limit.OnDeviceLimitProviderService"
            android:exported="false" />
        <service
            android:name=".revision3.watchzone.WatchZoneUpdateJob"
            android:permission="android.permission.BIND_JOB_SERVICE"
            android:exported="false"/>
        <activity
            android:name=".revision3.LandingActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".revision3.utils.TestActivity"
            android:label="@string/title_activity_test">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".revision3.UserZonesActivity"
            android:label="@string/title_user_zone_activity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".revision3.WatchZoneExplorerActivity"
            android:label="@string/title_watch_zone_explorer_activity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".revision3.WatchZoneDetailsActivity"
            android:label="@string/title_watch_zone_details_activity"
            android:screenOrientation="portrait" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <!--
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait" />

        <receiver
            android:name=".BootupReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".BootupSchedulerService"
            android:exported="false" />
        -->
        <!--
        <service
            android:name=".ActivityDetectionService"
            android:exported="false" />
        <service
            android:name=".SweeperService"
            android:enabled="true"
            android:exported="false" />
        -->

        <!--

        <service
            android:name=".model.watchzWatchZoneUpdateServiceTODOvice"
            android:exported="false" />
        <service
            android:name=".model.watchzone.WatchZoneNotificationDeliverer"
            android:exported="false" />
        <service
            android:name=".model.AddressValidatorService"
            android:exported="false" />

        <receiver
            android:name=".model.watchzone.WatchZoneAlarmReceiver"
            android:enabled="true"
            android:exported="false" />

        <activity
            android:name=".presentation.manualalarms.CreateWatchZoneActivity"
            android:label="@string/title_activity_maps"
            android:screenOrientation="portrait" />
        <activity
            android:name=".presentation.manualalarms.WatchZoneDetailsActivity"
            android:label="@string/title_activity_maps"
            android:screenOrientation="portrait" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings"
            android:screenOrientation="portrait" />
        <activity
            android:name=".DebugActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".LandingActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.manualalarms.WatchZoneViewActivity"
            android:label="@string/title_activity_alarm"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
        -->
    </application>

</manifest>